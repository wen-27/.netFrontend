import { FormShell } from "../../../shared/components/layout/FormShell";

export function PartPurchaseCreatePage() {
  return <FormShell title="Nueva compra de repuestos" description="Proveedor, fecha, repuestos, cantidades y costos." primaryLabel="Proveedor" secondaryLabel="Número de compra" />;
}
