import { FormShell } from "../../../shared/components/layout/FormShell";

export function UserRolesPage() {
  return <FormShell title="Asignar roles" description="Administra roles y permisos del usuario seleccionado." primaryLabel="Usuario" secondaryLabel="Roles" />;
}
